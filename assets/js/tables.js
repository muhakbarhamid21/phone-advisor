/* UNTUK ANALYSIS USER */

document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel1");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel2");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel3");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel4");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel5");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel6");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel7");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel8");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel9");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel10");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel11");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});

/* UNTUK EDIT CRITERIA */

document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel12");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});

/* UNTUK EDIT ALTERNATIVE */

document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel13");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});

/*  */

document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel14");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
document.addEventListener("DOMContentLoaded", function () {
  const table = document.getElementById("tabel15");
  if (table) {
    new simpleDatatables.DataTable(table, {
      paging: false, // Menonaktifkan pagination (entries per page)
      searching: false,
      searchable: false,
      sortable: false, // Menonaktifkan fitur pencarian
      info: false, // Menonaktifkan informasi jumlah entries
    });
  }
});
